# v6 版本说明

> 记录 `v5.5.html -> v6.html` 的改动清单。  
> 目标：在保留原架构和功能的前提下，把 UI、信息层级和甘特图规则整体升级。

## 源文件关系

- 基线：`C:\Users\15421\Desktop\v5.5.html`
- 目标：`C:\Users\15421\Desktop\v6.html`
- 方向：同一套功能骨架，换成更柔和、更统一的工作台视觉

## 主要改动

### 1. 全局视觉统一

- 字体改为更柔和的工作台字体组合。
- 卡片、筛选栏、弹窗、按钮、批量录入、报告块统一圆角化。
- 浅底、细边框、轻阴影成为主视觉。
- 交互 hover 改得更轻，整体更顺。

### 2. 概况页 UI 重整

- 快速录入区域改为更柔和的输入块。
- 报告生成区与报告展示区统一成同一套卡片语言。
- 概况页各模块之间的留白和层级更清晰。

### 3. Roadmap 页面收束

- 表格密度下降，但信息逻辑保持原样。
- 任务名称列按 `10` 个字符处理，其他列按更短展示规则截断。
- 删除任务列表中的「编辑」按钮，仅保留双击编辑。
- `+子` 继续保留。
- 任务类型、版本号、状态点、项目卡片都改成更柔和的圆角样式。
- 左侧导航保持固定，滚动时仍可见。

### 4. 甘特图规则重做

- 甘特图从“只按实际日期”改为“连接起始日期到实际交付日期”。
- 红条上优先显示「天数 + 任务名称」。
- 如果空间不够，就只保留「天数」。
- 通过渲染后的宽度检测，避免半截任务名。
- 保持红色视觉主导，信息更直接。

### 5. 小范围交互修正

- 图例、状态点、标签、卡片 hover 等统一到同一套视觉体系。
- 报表、任务表、Roadmap、甘特图之间的风格差异收窄。
- 仍然不改原有模块和数据结构。

## 结果

- `v6.html` 是 `v5.5` 之后的视觉和交互升级版。
- 它不是新产品，而是同一产品的 UI 重构版。
- 这个版本更适合作为后续公开展示或作品集参考稿。
