# v6 更新日志

> 本文件记录 **v6** 版本的迭代更新（含 v5.5 原版与 Codex UI 大改版的合并记录）。每次修改后追加对应条目，含需求、改动点、验证方式。
> 基线：`work-buddy-version.html`（单文件纯静态 HTML + CSS + 原生 JS，localStorage 持久化 `campus_workbench_v1`）。

---

## v6 会话更新记录（2026-07-29）

### 1. 模态框关闭改为「按下 + 抬起」双重认证 + 日期「当日」按钮

**需求①：模态框关闭改为双重认证**
- 原逻辑：`modalBg` 的 `click` 事件，点一下背景就关闭——弹窗内拖选文字极易误触关闭、丢失编辑内容。
- 新逻辑：`mousedown` 记录是否按在蓝色背景（`#modalBg`）上；`mouseup` 时仅当「按下」与「抬起」都落在背景外才 `closeModal()`。
- 代码位置：`Modal` 区，`_modalDownOnBg` 标记 + `mousedown`/`mouseup` 双监听（移除原 `click` 关闭）。

**需求②：所有日期选择器新增「当日」按钮**
- 覆盖范围：添加任务（起始/预计交付/实际交付）、批量添加（起始/预计交付）、编辑任务（起始/预计交付/实际交付）。
- 行为：
  - 起始时间旁的「当日」→ 填入**今天**（`todayStr()`）
  - 预计交付 / 实际交付旁的「当日」→ 填入**起始日期**字段的值（`data-fill="ref"`）
- 实现：日期 `input` 与「当日」按钮同行 flex 布局；`modalBg` 上统一事件委托 `.today-btn`，按 `data-target`（参照输入框）+ `data-fill`（today/ref）决定填入值。

**验证**：语法检查 ✅ ／ 本地预览 HTTP 200 ✅ ／ 断言（`_modalDownOnBg`×3、旧 `click` 关闭已移除、`today-btn`×8、`data-fill` today×3 + ref×4）✅

---

### 2. 双击任务行直接打开编辑

- 看板（Roadmap 表格）每行 `<tr data-id>` 增加 `dblclick` 监听 → 调用 `openEdit(id)`。
- 防误触：双击落在操作按钮（`button`）或母任务展开箭头（`.tw-toggle`）上时**不**触发，避免与「编辑 / ＋子 / 展开收起」冲突。
- 交互提示：任务行加 `cursor:pointer` + hover 高亮，提示可双击。
- 代码位置：`renderRoadmap()` 行监听区（紧邻 `[data-edit]`/`[data-toggle]` 绑定之后）。

**验证**：语法检查 ✅ ／ HTTP 200 ✅ ／ 断言（`addEventListener('dblclick'` + `openEdit(tr.dataset.id)` 命中）✅

---

### 3. 任务类型可编辑（全局管理，编辑页内只读）

- `TYPES` 常量 → 改为动态 `getTypes()`，数据来源 `state.customTypes`（持久化，旧数据经 `normalize()` 补齐，不丢）。
- 新增 `openTypeEditor()` 弹窗：可**新增 / 重命名 / 删除**类型；删除某类型后，已用该类型的任务与文件自动归入「其他」。
- 「编辑类型」按钮仅加在**三处**任务类型旁（符合需求，不在单任务编辑页）：
  1. 添加任务（快录确认弹窗）`#f_editType`
  2. 批量添加（每张卡片）`.bedit-type`
  3. 任务列表分类筛选栏 `#filter_editType`
- **单任务编辑弹窗的任务类型改为 `disabled` 只读**（仅可选、不可改），并加 `title` 说明去处。保存时 `disabled` 的 select 仍能通过 `.value` 读回原值，不丢数据。
  > ⚠️ **§7 已修正**：用户澄清「双击后可换类型，只是不能编辑类型」，已撤销 `disabled`，恢复为普通 `<select>`（不放「编辑类型」按钮即可）。
- 文件库类型筛选**不加**此按钮（不在指定三处范围内）。

**验证**：语法检查 ✅ ／ HTTP 200 ✅ ／ 断言（`function openTypeEditor`×1、三处「编辑类型」按钮 + 编辑页禁用说明、`e_type disabled`、`customTypes`×7 处引用）✅

---

### 4. 甘特图显示优化

- **去掉项目名前缀**：不再渲染 `【项目名】`，条内只保留任务名。
- **按条宽分级显示**（`barHTML` 内 `width` 百分比判断）：
  - 窄条（`width < 10%`，即工期很短）→ 只显**日期区间**（`M/D–M/D`）
  - 中条（`width < 24%`）→ 只显**工期**（`N天`）
  - 宽条 → 显**工期 + 任务名**（名称超长由 CSS `text-overflow:ellipsis` 截断）
- **鼠标悬停**任意条统一显示完整任务名（bar 上加 `title="${任务名}"`）。
- 清理失效的 `.gantt-bar .proj` / `.gantt-row.child .gantt-bar .proj` 死 CSS。
- 新增 `.g-date` 样式（窄条日期区间）。

**验证**：语法检查 ✅ ／ HTTP 200 ✅ ／ 断言（proj span 已从 HTML 移除、残留 `.proj` CSS 归零、分级 `inner` 逻辑 + `.g-date` 存在）✅

---

### 5. 看板列宽缩短（除任务名称外最多约 8 字）

- 表格 `td` 的 `max-width` 由 `280px` 改为 `112px`（约 8 个中文字），超长截断加省略号（`text-overflow:ellipsis` 沿用）。
- 任务名称列 `td.name-cell` 设 `max-width:none` 不限宽，吸收剩余空间。
- 各列 `title` 属性保留，悬停看全文。

**验证**：语法检查 ✅ ／ HTTP 200 ✅ ／ 断言（`max-width:112px`×1、`td.name-cell{max-width:none}`×1）✅

---

### 6. 数据导入导出（4.7：换设备 / 清缓存不丢）

- 概况页（№01）新增「数据备份」卡片，含 **导出备份** 与 **导入备份** 两个按钮（导入用隐藏 `file` input）。
- **导出** `exportData()`：把整个 `state` 序列化为 `.json` 下载（`work-buddy-备份-YYYYMMDD.json`），一键本地留存。
- **导入** `importData()`：读文件 → `JSON.parse` 校验 → 弹窗 `openImportChoice()` 让用户二选一：
  - **覆盖** `impOverwrite`：以备份为准，整份替换当前 `state`（`normalize()` 补齐字段，旧数据结构不丢）。
  - **合并** `impMerge`：两份数据按 `id` 去重拼合（`mergeState()`，同 id 以备份覆盖），`projectOrder` / `customTypes` 取并集。
- 导入前提示先导出当前数据，防误操作。
- 复用既有 `state` / `normalize()` / `save()` / `render()`，无新依赖、符合 SPA 结构。

**验证**：语法检查 ✅ ／ HTTP 200 ✅ ／ 断言（`exportData`/`importData`/`openImportChoice`/`mergeState` 各×1、概况页 `expBtn`/`impBtn`×1、`impOverwrite`/`impMerge`×2）✅

---

---

### 7. 甘特图条形样式修复 + 编辑页类型选择恢复可改

**问题：甘特图条形丢失样式**
- 用户反馈甘特图「出bug了」——红色条形背景消失，变成散落的纯文本（"N天"、"任务名"无容器、无颜色）。
- 排查：
  - CSS `.gantt-bar` 规则完整存在（L158），括号平衡（219/219），属性齐全。
  - JS `barHTML` 输出的 `<div class="gantt-bar">` 结构正确。
  - **根因**：CSS 规则虽在 `<style>` 中且语法无误，但浏览器渲染时 `.gantt-bar` 选择器未能生效（疑似 CSS 解析/级联上下文问题）。headless Chromium 截图确认：元素存在但无任何 `.gantt-bar` 样式。
- **修复**：将所有关键视觉属性以 **inline style** 直接写在 `gantt-bar` / `gantt-bar.summary` 的 `<div>` 上：
  - `position:absolute;top:6px;height:24px;background:var(--accent);color:#fff;border-radius:4px;display:flex;align-items:center;padding:0 10px;gap:6px;box-shadow:0 2px 6px rgba(230,57,70,.28);overflow:hidden;white-space:nowrap;z-index:3`
  - summary 条额外加 `opacity:.5;border:1px dashed #fff;cursor:pointer`
- 效果：inline style 优先级最高，彻底绕过 CSS 级联问题。截图验证 ✅ 红色圆角条形恢复。

**修正：编辑页类型选择恢复可选**
- §3 中曾将编辑弹窗的 `<select id="e_type">` 设为 `disabled` 只读。用户澄清：「双击任务之后可以选择更换类型，只是不能编辑类型而已」。
- 已撤销 `disabled`，恢复为普通 `<select>`（仍从 `getTypes()` 列表选取，只是编辑弹窗内不放「编辑类型」按钮）。

**验证**：语法检查 ✅ ／ Chromium headless 截图对比（修复前纯文本 vs 修复后红条圆角）✅ ／ 断言（两处 gantt-bar 均含完整 inline style、`e_type` 无 disabled 属性）✅

---

### 8. Codex 修改版 UI 统一：柔和工作台风格覆盖到主要窗口

**需求：保持原 HTML 架构和功能，只做审美/UI完善**
- 工作文件固定为：`codex修改ui-对应v5版本.html`，不再生成额外备份文件。
- 保留原有 SPA 结构、模块、数据字段、localStorage 持久化和任务/文件/报告逻辑，不重做产品架构。
- 在原 CSS 后追加统一视觉覆盖层，采用「柔和工作台」方向：
  - 全局字体统一为 `Aptos / Segoe UI Variable Text / Segoe UI / PingFang SC / Microsoft YaHei`。
  - 卡片、筛选栏、弹窗、批量录入卡片、报告卡片、项目卡片统一为浅底、圆角、细边框、轻阴影。
  - 按钮统一为圆角胶囊形，hover 使用轻微上浮和柔和阴影。
  - 快速录入框改为圆角浅色输入区，去掉硬黑边框。
  - 报告生成后的展示卡片统一为柔和圆角卡片，保留左侧红色强调线。
- 左侧导航保持固定视觉与工作台结构，不随主内容滚动丢失入口。

**版本备注**
- 这部分不是单点修补，而是从 `v5.5.html` 过渡到 `codex修改ui-对应v5版本.html` 的主要视觉改造。
- 两版的功能骨架保持一致，但 Codex UI 版把整体审美、卡片层级、交互反馈和信息密度都重新收束了一遍。

**验证**：语法检查 ✅ ／ 本地预览 HTTP 200 ✅ ／ Edge 截图检查（概况页、报告生成区）✅

---

### 9. Roadmap 表格与交互收束：按原 HTML 逻辑保留，压缩显示密度

**需求：功能逻辑完全按原 HTML，只优化视觉和可用性**
- Roadmap 表格保留原有母/子任务结构、筛选、排序、状态、进度、交付成果、反馈、难点、负责人、起始/预计/实际交付日期等字段。
- 任务名称列保持 `10个字符 + ...` 的展示规则，完整名称保留在 `title` 悬停提示中。
- 任务要求及其他非任务名字段保持 `最多8个字符 + ...` 的展示规则，减少 Edge 75% 缩放下的横向拥挤。
- 删除任务行中的「编辑」按钮，仅保留：
  - 双击任务行打开编辑；
  - 「+子」按钮新增子任务。
- 状态不再额外使用多余点点符号，主要依靠前置黑点/红点/浅色点表达。
- 任务类型、版本号、母子任务标识等 pill 样式改为圆角柔和标签，避免直角硬块。
- Roadmap 项目卡片与直角块统一换成立体卡片表现，hover 有轻微上浮动效。

**验证**：语法检查 ✅ ／ Edge 截图检查（Roadmap 项目页、表格页）✅ ／ 操作检查（双击编辑、+子保留）✅

**版本备注**
- `v5.5.html` 更接近功能基线和字段基线。
- `codex修改ui-对应v5版本.html` 是在不改架构前提下，对 Roadmap、表格密度、任务标签、按钮、卡片、甘特图可读性做的第二轮大改。
- 这不是单个按钮或单个控件的优化，而是整个页面的视觉语言升级。

---

### 10. 甘特图最终规则：起始日期连接实际交付日期，红条显示天数/任务名

**最终需求**
- 甘特图不再只按实际交付日期显示单点。
- 每条甘特红条应连接：`起始日期 → 实际交付日期`。
- 红条上优先显示：`天数 + 任务名称`。
- 如果任务名称不能完整显示，则不截断任务名，直接只显示：`天数`。

**实现**
- `ganttHTML()` 新增 `spanInfo(start, actual)`：
  - 只渲染同时填写了起始日期和实际交付日期的任务。
  - 计算当前月份范围内的可见起点、终点、宽度和持续天数。
  - 兼容实际交付日期早于起始日期的情况，视觉上仍按较早日期到较晚日期连接。
- `barHTML()` 改为使用 `t.start` + `t.actual` 生成红色连接条。
- 条内结构改为：
  - `<span class="g-days">N天</span>`
  - `<span class="g-name">【任务名称】</span>`
- 新增 `fitGanttLabels()`：
  - 渲染后实际测量红条宽度。
  - 如果 `天数 + 完整任务名` 放不下，则加 `.compact`，隐藏任务名，只保留天数。
  - 禁止出现半截任务名或乱码式挤压。
- 页面说明同步改为「红条连接起始日期与实际交付日期；条内优先显示天数和任务名称，放不下时只显示天数」。

**验证**
- JS 语法检查 ✅
- 本地预览 HTTP 200 ✅
- Edge 截图验证 ✅
  - 空间不足时：只显示 `4天`
  - 空间足够时：显示 `7天【整理招商局面试话术】`

---

## 当前状态

- 功能基线文件：`v5.5.html`。
- Codex UI 修改工作文件：`codex修改ui-对应v5版本.html`（承接 v5.5 功能逻辑，并追加以上 8–10 UI / Roadmap / 甘特图改动）。
- 部署入口：`work-buddy-version.html`（后续发布时应与确认后的 Codex UI 修改版同步）。
- 视觉规范、SPA 结构、`localStorage` 持久化、向后兼容均保持。
- 版本定位：v6。

## 后续计划（待办）

- 4.4 任务 ↔ 文件关联（`linkedFiles` 已预留）
- 4.5 全局搜索（跨 tasks/logs/notes/files）
- 4.8 响应式布局（移动端适配）
- 走 `work-buddy-deploy` 技能发布 v6

> 注：4.7 数据导入导出已在本会话完成（见上 §6），从后续计划移除。
