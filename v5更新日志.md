# v5 更新日志 · 校招全周期工作台（Work Buddy）

> 版本：**v5** ｜ 阶段：Phase 4 · 细致优化
> 核心交付：**母任务 / 子任务（树形折叠）** + 修复“无法添加子任务”致命 bug
> 入口：`work-buddy-version.html`（固定最新版）｜ 线上：`https://suul-chen.github.io/work-buddy-version/`

---

## 一、母任务 / 子任务（树形折叠）

- **各项目独立**：母 / 子任务完全按项目隔离——某项目的母任务只能在该项目下挂子任务，跨项目不会串。候选母任务列表按项目过滤。
- **常驻「＋子」按钮**：看板每行都显示 `＋子`，点击只在**当前母任务下**新建子任务（弹窗预填母任务 id），不再出现“点了没反应”。
- **两级级联（录入 / 批量录入一致）**：先选**项目** → 出现**一级母任务** → 选一级后出现**二级子任务**，弹出逻辑与其他弹窗统一，并给出“将作为《X》的子任务”提示。
- **子任务跟随母任务项目**：录入子任务时若未显式改项目，自动归入母任务所在项目；编辑母任务项目时，其下子任务同步跟随，保证整棵树在同一看板内。
- **树形渲染与折叠**：看板按归属关系递归渲染，母任务名可点击展开 / 收起子任务，母任务进度取子任务平均值；折叠状态持久化。
- **编辑隔离 + 防环**：编辑弹窗的“主任务”下拉仅列同项目候选，并排除自身及所有子孙，杜绝把任务挂成自己的子任务。

## 二、修复致命 bug：无法添加子任务 / 母任务“消失”

- **根因**：录入弹窗与看板筛选栏**共用了 `任务类型 / 起始时间 / 预计交付` 三个字段的 id**。`mSave` 读取时拿到的是 DOM 中先出现的**筛选栏元素**（初始为空），导致必填校验永远失败 → 子任务**根本没写入**。用户在弹窗里填了日期却保存不上，表象就是“不能添加子任务 / 加了母任务就没了”。
- **修复**：弹窗内所有字段读取统一限定在弹窗作用域（`#modalBg`），彻底隔离与筛选栏的同名冲突。
- **连带修复**：文件上传 / 编辑弹窗也存在同样的字段 id 冲突，一并修正（防御性）。
- **清理**：移除调试期遗留的日志与临时状态。

## 三、批量录入体验优化

- 概览修正窗口改为**两行式卡片**（元信息行 + 内容行），信息更紧凑、可对照修改。
- 缩短「任务要求」列宽，让任务名称列获得更多空间。

## 四、健壮性：升级不丢数据

- 修正 `normalize()` 中误删任务名的逻辑（此前会导致刷新后所有任务名变空），确保旧 `localStorage` 用户升级不丢数据（符合向后兼容规范）。

---

## 五、质量保障

- 新增 jsdom 端到端测试（22 条断言），覆盖：看板显示任务名、`＋子` 常驻、在母任务下加子任务后母任务保留且展开可见、录入 / 批量录入“先项目后母任务”、同级项目母任务互不串、两级级联出现二级下拉、编辑弹窗母任务候选按项目隔离、批量卡片级联仅列同项目。
- **结果：22 / 22 全部通过；加载 0 个 JS 错误；`localStorage` 正常写入。**
- 改动严格遵循规范：单文件静态、无构建无后端、`localStorage` 持久化、视觉与 SPA 结构不变、复用既有 `parse / normalize` 函数、无新增依赖。

---

*本版在 v4 基础上补完母任务 / 子任务主线能力；未尽事项转入 `v6更新日志.md` 计划。*
