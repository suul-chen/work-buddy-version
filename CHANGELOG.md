# 更新日志 · 校招项目全周期工作台（Work Buddy）

> 本文件记录每次部署到 GitHub Pages 的版本与变更。
> 站点入口：`https://suul-chen.github.io/work-buddy-version/`
> 仓库：`suul-chen/work-buddy-version` · `main` 分支根目录
> 部署原则：单文件纯静态 HTML（无构建、无后端），localStorage 持久化；只部署、不改应用代码。
>
> **版本命名规则**：每次部署版本号递增（v1 → v2 → v3 → v4 → v5 …），
> 同步生成 `v{N}.html` 历史快照 + Git Tag `v{N}` + GitHub Release。

---

## v4 · 2026-07-28 — Phase 4 批量录入 / 模糊索引 / 自动报告 / 项目名定稿

- **来源**：桌面版 `work-buddy-version.html`
- **文件指纹**：MD5 `6fce00af03ed179788af07a8dc278340` · 86,833 bytes
- **变更摘要**：
  - ✅ **4.1 批量录入**：新增 `parseBatch(text, refDate)` 上下文栈解析（时间上下文 / 项目分组 / 编号条目 / 局部时间片段）；批量录入弹窗 `openBatchModal`（大文本框 + 解析预览表，支持逐条编辑 / 删除 / 改归属，不确定项标黄）；确认后批量写 tasks 并记汇总日志
  - ✅ **4.2 关键词模糊索引**：新增项目别名表 `PROJECT_ALIASES`（特发↔特发集团、高薪投↔高新投、平安产险↔中国平安）+ 类型别名表 `TYPE_ALIASES`（校招启动文/简章/推文/海报→文案宣传类，ppt/呈现→设计类 等 6 大类）；`normalizeProject / normalizeType` 在录入时自动归一（含 includes 子串匹配）
  - ✅ **4.6 自动化报告增强**：`buildReport(type)` 改为按时间自动归类（日报=今日交付+进行中+待办；周报=本周区间）；弹窗顶部新增量化摘要；新增定时提醒 `checkReportReminder`（周一~周四 17:50 起草日报、周五 17:30 起草周报，同类型当天不重复）
  - ✅ **4.9 项目名确认**："高薪投/高新投"统一归一为**高新投**；seed 与 parseQuick/parseFile 识别词表已同步更新
  - 🔧 **甘特图飞书式拖拽平移**：弃点击切换周，改为鼠标/触摸/滚轮拖拽 translateX 平移时间轴
  - 🔧 **月视图 only**：去掉周视图与"今日"单位，甘特图仅保留按月
  - 🔧 **崩溃修复**：修复日报/周报/代办生成后概况页卡死（根因：todos.name.todo → todos.content.todo）
- **部署方式**：PAT + GitHub API 推送至 `suul-chen/work-buddy-version`（main）→ GitHub Pages 已启用
- **状态**：✅ 已上线
- **详细总结**：见 [v4项目总结.md](./v4项目总结.md)

---

## v3 · 2026-07 — Phase 3 文件库（飞书链接 / 文件关联 / 版本留痕）

- **来源**：桌面版 `index.html`（v3 迭代版）
- **文件指纹**：MD5 `e336f13dcca64a3461c1a68bf4789351` · 64,478 bytes
- **变更摘要**：
  - ✅ **Phase 3 文件库完整落地**：
    - `files` 模型新增 `url` 字段；`normalize()` 自动补齐旧数据
    - `parseFile()` 新增飞书分享链接正则识别（粘贴链接自动分离出文件名）
    - 文件录入弹窗新增「分享链接」输入框；必填校验：文件名 / 项目 / 文件类型 / 上传日期
    - 文件表：文件名带 url 时渲染为蓝色可点击 ↗，新标签页打开飞书在线预览/下载
    - 文件表每行新增「编辑 / 删除」按钮，操作后自动写工作日志
    - seed 内置真实飞书文档链接作为示例数据
  - ✅ **任务 ↔ 文件关联预留**：`linkedFiles` 字段在 normalize() 中预置补齐（UI 待接）
  - ✅ **任务版本迭代留痕**：新增 `version` / `feedback` 字段，追踪任务变更历史
  - 🔧 **甘特图交互增强**：新增飞书式拖拽平移（translateX）
- **部署方式**：首次通过 GitHub API 部署
- **状态**：✅ 已归档（v4 已覆盖）
- **详细总结**：见 [v3项目总结.md](./v3项目总结.md)

---

## v2 · 2026-06 — Phase 2 基础页面交互完善

- **来源**：桌面版 `index.html`（v2 迭代版）
- **文件指纹**：MD5 `e8ad211fa5966c939884e907a40fea01` · 53,841 bytes
- **变更摘要**：
  - ✅ **五大模块全部落地可交互**：
    - 概况 №01：快录框 `parseQuick()` 自然语言识别 → 弹窗确认 → 自动写日志；本周指标卡；今日代办；最近动态；报告生成入口
    - Roadmap №02：项目卡片网格（可拖拽排序）→ 项目专属看板；MS Project 风格甘特图（周/月视图切换、今日线）；筛选排序栏；任务编辑/删除
    - 文件库 №03（初代）：文件快录 `parseFile()`；筛选栏；文件列表展示
    - 碎碎念 №04：随手记录，不进日报
    - 工作日志 №05：自动记录每次操作，按天分组 + 搜索
  - ✅ **报告系统**：日报 / 周报 / 代办生成与确认，确认后可转待办
  - ✅ **单条自然语言识别**：`parseQuick()` 识别项目/类型/日期；`parseFile()` 识别文件字段
  - 🔧 筛选/排序栏优化；本周指标卡
- **备注**：v2 与 v1 文件内容一致（同期迭代，未产生功能差异快照）
- **状态**：✅ 已归档

---

## v1 · 2026-06 — Phase 1+2 基线框架（骨架 + 基础交互）

- **来源**：桌面版 `index.html`（初始交付版）
- **文件指纹**：MD5 `e8ad211fa5966c939884e907a40fea01` · 53,841 bytes
- **变更摘要**（首版，从零到一）：
  - ✅ **Phase 1 基础框架搭建**：
    - 单文件 HTML+CSS+原生 JS，无构建、无后端
    - SPA 路由：`go(route,project)` + `setMain(html)`；view-enter 入场动画
    - 数据模型 `state`：tasks / logs / notes / reports / files / projectOrder / filters / fileFilters
    - 标签体系：任务内容、类型、项目、起始/预计/实际交付、进度、交付成果、关联任务、难点和动作、负责人、父记录、状态
    - 视觉风格：poster / 编辑风海报（暖米底 #efeae0、近黑字 #0a0a0a、强红 #e63946、编号页眉 №01~№05）
    - `seed()` 示例数据 + `normalize()` 兼容旧数据
    - `load()/save()` localStorage 持久化
  - ✅ **Phase 2 基础页面交互（初版）**：
    - 五大模块页面可切换（概况 / Roadmap / 文件库 / 碎碎念 / 工作日志）
    - 单条任务录入闭环（自然语言识别 → 弹窗确认 → 写日志）
    - 基础看板 + 甘特图（周/月视图）
    - 基础报告生成（日报/周报/代办）
  - 📌 此时文件库仅为初代（无飞书链接、无 URL 字段）
- **部署方式**：本地交付
- **状态**：✅ 已归档

---

## 部署工作流（给后续交付）

1. 用户交付新版本 `index.html`（桌面版）。
2. 校验 MD5；与线上一致则无需重新部署，仅补记日志。
3. 不一致时：
   - 复制到仓库根 `index.html`（与 `work-buddy-version.html` 保持一致）
   - 同时存一份 `v{N}.html` 作为历史快照
   - 提交并 push（PAT + GitHub Contents API）
4. 追加一条本文件记录（版本 / 日期 / 来源 / 指纹 / 变更摘要）。
5. 打 Tag `v{N}` + 创建 GitHub Release（作为平台级"更新信息"留存）。
6. 验证 `https://suul-chen.github.io/work-buddy-version/` 返回 200。

> 下一个版本号：**v5**
