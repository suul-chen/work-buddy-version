# v6.5 更新日志 · 校招全周期工作台（Work Buddy）

> 版本：**v6.5**（本地未部署，线上站点仍为 v6）｜ 阶段：Phase 4 · 体验打磨补丁
> 定位：v6 之上的体验打磨补丁线（看板排序 / 进度控件 / 状态统一 / 编辑体验 / 指标卡详情 / 日志留痕 / 旧记录兜底）
> 入口：`work-buddy-version.html`（固定最新版）

---

## 变更摘要

### 1. 版本号排序优化
- **问题**：看板表格中同一任务的多个版本（v1、v2、v3）按起始时间排列，版本顺序混乱
- **改动**：组内排序逻辑改为「先按 `verNumOf(version)` 升序，再按起始时间兜底」
- **效果**：同一任务的不同版本始终按 v1 → v2 → v3 从上到下排列

### 2. 整体进度控件升级
- **改动**：编辑弹窗「整体进度」从纯数字输入框改为——左侧输入框 + 右侧 4 个快捷按钮（25 / 50 / 75 / 100）
- **效果**：常用进度值一键填入，保留手动输入灵活性

### 3. 状态术语统一
- **改动**：全局从「未完成 / 正在进行 / 已交付」统一为 **「未开始 / 进行中 / 已交付」**
- **涉及**：STATUSES 常量、CSS 类名、13 处硬编码比较、seed 数据、图例/指标卡/报告标签

### 4. 编辑弹窗文本字段升级为 textarea（自适应高度）
- **问题**：「反馈意见」「难点和动作」使用单行 input，长文本无法完整查看和编辑
- **改动**：两字段改为 `<textarea class="auto-resize">`，随内容自动增高；下方字段自动下移
- **效果**：输入多行文本时完整可见，随时方便修改

### 5. 修复任务名含 vN 被误删的 bug
- **问题**：用户输入"修改v2"作为任务名时，`baseNameOf()` 强制截断为"修改"，丢失语义
- **改动**：
  - `openQuickPreview()` 中名称输入框显示原始输入（不再用 baseNameOf 截断）
  - `baseNameOf()` 仅用于后台匹配已有任务组（判断版本归属）
  - 快速录入保存和批量录入保存均使用用户原始名称
- **效果**：用户输入什么就存什么，版本号由独立字段管理

### 6. 概况页四指标卡点击弹出详情弹窗
- **新增功能**：概况页「本周任务总数 / 已交付 / 进行中 / 未开始」四个指标卡可点击
- **弹窗内容**（wide 弹窗，任务条形式）：
  - **任务总数**：按状态分组（未开始→进行中→已交付），每组内按项目分组；每条显示名称/要求/起始/预计交付/实际交付（仅已交付）
  - **本周已交付**：按项目分组；每条显示名称/交付成果/起始/预计交付/实际交付
  - **本周进行中**：按项目分组；每条显示名称/要求/起始/预计交付
  - **本周未开始**：按项目分组；每条显示名称/要求/起始/预计交付
- **交互**：点击任务条可跳转到对应项目的看板视图

### 7. 跳转后目标任务条闪烁两次（最终：回退为初版等速双闪）
- **新增交互**：指标卡详情弹窗、工作日志详情弹窗中的「前往该任务」跳转后，目标任务条高亮闪烁两次，辅助用户定位
- **迭代**：曾尝试「第一次快、第二次缓」的双峰单动画，观感不佳，已按需求**回退为初版等速双闪**
- **最终实现**：`@keyframes rowFlash`（单关键帧，50% 处高亮）+ `tr.flash{ animation: rowFlash .55s ease 2 }`，即两次**等速**闪烁；清理逻辑为单次 `animationend` 移除 class
- **避坑**：早期曾用 `rowFlash` + `rowFlashSlow` 两段动画作用于同一组属性，被 CSS「最后一个胜出」规则覆盖（实际只在延迟后闪一次慢的），现统一为单关键帧规避
- **效果**：跳转后目标任务行闪烁两次（等速），辅助定位

### 8. 工作日志「留痕」信息补全（新增内容可见）
- **问题**：新增类操作（新建任务 / 批量录入 / 上传文件）只记录一句摘要，打开卡片后看不到具体填了什么
- **改动**：
  - 新增 `snapshotFields(obj, fields)`：把新对象的关键字段拍成「（空）→ 值」快照
  - 新增 `ADD_SNAPSHOT_FIELDS` 字段清单（类型/项目/起始/预计交付/状态/进度/版本/负责人…）
  - `addTask` / `batchAdd` / 文件上传三处 `addLog('add'/'file', …)` 改用快照作为 `changes`
  - `openLogDetail()` 详情弹窗顶部新增摘要说明行（`.log-dt-sum`），下方保留「前 → 后」差异表
  - 新增 `diffRowHTML()`（前→后差异行）、`kvRowHTML()`（当前值行）渲染助手
- **效果**：
  - **新增**：打开卡片可见「（空）→ 实际填的值」，即新增了哪些内容
  - **修改**：原本就记录「前 → 后」差异，现在卡片顶部再加一句摘要，上下文更完整
  - 小卡片摘要由最多 2 项提升到 3 项（`logChangeSummary`）

### 9. 旧日志兜底展示（打开卡片信息不再少）
- **问题**：功能上线前创建的日志 `changes` 为空，打开卡片只剩一句话摘要，看不到内容
- **改动**：`openLogDetail()` 新增**兜底分支**——若无 `changes` 但关联到现存任务，则调用 `snapshotFields()` 展示该任务**当前信息**（键值行 `.kv-row` + 说明 `.log-note`）；无关联任务的旧日志仍显示摘要
- **效果**：旧记录（无论修改/新增）打开也能看到关联任务当前信息，不再空白

---

## 技术细节

| 改动 | 文件位置 | 类型 |
|------|---------|------|
| 版本号排序 | 看板 `byParent` 组内 sort 逻辑 | JS |
| 进度快捷按钮 | 编辑弹窗 `e_prog` + 事件绑定 | HTML + JS |
| 状态重命名 | STATUSES / CSS / 13+处引用 | 全局 |
| textarea 自适应 | 编辑弹窗 e_fb/e_diff + auto-resize CSS/JS | HTML + CSS + JS |
| baseNameOf 隔离 | openQuickPreview / 快速保存 / 批量保存 | JS |
| 指标卡详情弹窗 | openMetricDetail() + dt-* CSS + 点击绑定 | JS + CSS |
| 闪烁双闪 | `@keyframes rowFlash`（单关键帧等速双闪）+ `tr.flash{ animation: rowFlash .55s ease 2 }` + `highlightTaskRow()` 清理 | CSS + JS |
| 新增留痕快照 | `snapshotFields()` / `ADD_SNAPSHOT_FIELDS` + 三处 addLog | JS |
| 差异 / 键值行助手 | `diffRowHTML()` / `kvRowHTML()` + `openLogDetail()` 摘要行 | JS + CSS |
| 旧日志兜底 | `openLogDetail()` 无 `changes` 时按 `targetId` 展示任务当前信息 | JS + CSS |
| 新增样式 | `.log-dt-sum` / `.log-note` / `.log-kv` / `.kv-row` / `.kv-k` / `.kv-v` | CSS |

## 验证

- ✅ JS 语法检查 `node --check` 通过
- ✅ 本地预览 HTTP 200
- ✅ 抽取文件内**真实函数**模拟：新增日志生成「（空）→ 值」快照；修改日志生成「前 → 后」差异；旧日志（无 changes）+ 关联现存任务 → 输出任务当前信息键值行；旧日志无关联 → 输出摘要

## 向后兼容

- ✅ 纯行为变更，无新增字段
- ✅ 旧数据中 status 值在编辑保存后自动迁移到新选项
- ✅ localStorage 键名与数据结构不变
- ✅ 旧日志（升级前创建）`changes` 为空时，打开卡片按关联任务展示当前信息兜底（不再"只有一句话"）；新操作起自动记录前后内容

## 备注

- 本补丁线的改动均落在本地 `work-buddy-version.html`，**尚未部署**（GitHub 连接器令牌只读，push 被 403 拦截）。线上 `https://suul-chen.github.io/work-buddy-version/` 仍为 v6。
- 本批改动体量属于 v6 之上的体验打磨补丁，故归并为 **v6.5**；不再单独以 v7 计版本。
