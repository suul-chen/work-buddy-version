# 项目指令 · 校招全周期工作台（Work Buddy）

> 本指令用于规范「校招全周期工作台 / Work Buddy」项目的跨对话协作：**每个对话一开始就先到 GitHub 同步最新进度，再基于用户目标做完善改进与代码规范化。**
> 本文件本身以 GitHub 仓库中的最新版为准；需要更新时直接改仓库里的 `项目指令.md`，后续对话会自动读到最新内容。

---

## 1. 对话开始必做：同步 GitHub 进度（最高优先级）

**不要用记忆里的旧状态代替实时进度。** 每次对话启动时，先到 GitHub 读取以下文件，建立"当前进度"心智模型：

- **仓库**：`suul-chen/work-buddy-version`（分支 `main`）
- **线上站点**：`https://suul-chen.github.io/work-buddy-version/`
- **必读文件**（用 GitHub Contents API 或 WebFetch 拉 `raw.githubusercontent.com/suul-chen/work-buddy-version/main/<文件>`；中文文件名需 URL 编码）：
  - `项目指令.md` —— 本文件（始终以线上最新版为准）
  - `README.md` —— 产品定位、功能、技术架构
  - `CHANGELOG.md` —— 跨版本更新记录
  - `vN更新日志.md`（N = 最新版本号，当前 v4）—— 该版本阶段总结（按 4.1→4.9 顺序，未完成项即"后续计划"）
  - 最新版 HTML：`work-buddy-version.html`（可用 raw 拉取核对当前代码）

> 同步完成后，再响应用户当次需求；若用户目标与已记录进度冲突，以最新进度 + 用户当次明确目标为准。

---

## 2. 项目目标（用户意图）

- **产品**：校招 / 实习场景的**可视化项目管理工作台**，把多个求职项目的任务、交付物、过程留痕统一管理，从调研到落地形成可复盘、可量化的全周期记录，可作为个人作品集。
- **当前阶段**：Phase 4 · 细致优化（进行中）。
- **已完成**：
  - Phase 1 基础框架、Phase 2 基础交互、Phase 3 文件库
  - Phase 4 的 **4.1 批量录入 / 4.2 关键词模糊索引 / 4.6 自动化报告 / 4.9 项目名确认**
- **待完善（建议优先级）**：
  1. **4.4 任务 ↔ 文件关联**（字段 `linkedFiles` 已预留，投入产出比最高）
  2. **4.5 全局搜索**（跨 tasks/logs/notes/files 关键词 + 全文检索）
  3. **4.3 母任务 / 子任务**（`parent` 字段实现树形折叠）
  4. **4.7 数据导入导出**（JSON 导出/导入，换设备不丢）
  5. **4.8 响应式布局**（移动端适配）

---

## 3. 完善与改进的工作方式

1. **先同步进度**（第 1 节），确认当前代码状态与待办，不凭空假设。
2. **定位模块**：对照用户目标，确定要改的左侧导航模块——概况 №01 / Roadmap №02 / 文件库 №03 / 碎碎念 №04 / 工作日志 №05。
3. **改动遵循代码规范**（第 4 节）；改完必须做：**语法检查 + 本地预览 HTTP 200 + 针对性断言**。
4. **记录更新**：代码改动后，更新对应 `vN更新日志.md`（跨版本则新增），追加 `CHANGELOG.md`，必要时更新 `README.md`。
5. **部署**：使用 **work-buddy-deploy 技能** 完成发布（不要手写 git push），详见该技能与 `项目资产/部署细则.md`。

---

## 4. 代码规范（全局约束，必须遵守）

- **形态不变**：单文件纯静态 HTML + CSS + 原生 JS，**无构建、无后端**，浏览器 `localStorage` 持久化（键 `campus_workbench_v1`）。
- **基线唯一**：以 work buddy 版为唯一演进版本，不再分叉（桌面版 / sandbox 版均已并入）。
- **视觉不变**：poster / 编辑风——暖米底 `#efeae0`、近黑字 `#0a0a0a`、强红 `#e63946`、编号页眉 №01~№05、`view-enter` 入场动画。
- **向后兼容**：任何字段新增都经 `normalize()` 补齐，旧 localStorage 用户**不丢数据**。
- **可修改性**：所有自动识别的信息都必须可手动改（识别可能错）。
- **SPA 结构**：`go(route,project)` + `setMain(html)` 路由；`state` 对象管理数据；`load()/normalize()/save()` 持久化；`seed()` 示例数据。
- **复用优先**：新增功能优先复用既有函数（`parseQuick / parseFile / parseBatch`、`normalizeProject / normalizeType` 等），保持命名与逻辑风格统一；避免引入新依赖或框架。

---

## 5. 版本与文件约定

- 版本号每次发布 +1（v1 → v2 → …）；`work-buddy-version.html` 始终 = 最新版（固定入口）；`vN.html` 为各版本**永久快照**，只增不删。
- 阶段记录命名 `vN更新日志.md`（每版新增，原称"项目总结"）。
- 文档：`README.md`（产品文档）、`CHANGELOG.md`（跨版本日志）、`项目指令.md`（本文件）。
- **边界**：个人主页仓库 `suul-chen.github.io` 仅含个人主页 `index.html`，**严禁**把项目文件放进去。

---

## 6. 部署

- 使用 **work-buddy-deploy** 技能完成发布（含版本快照、覆盖最新版、写更新日志、打 Tag/Release）。
- 部署细则详情见 `项目资产/部署细则.md`（本地）或本仓库历史提交。
- **不要用 git CLI 直接 push**（沙箱凭据助手会拦截）；一律走 Contents API（技能已封装）。

---

## 7. 一句话守则

**开局先读 GitHub，再按目标改，改完规范检查，部署走技能。**
