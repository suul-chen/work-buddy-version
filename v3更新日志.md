# 校招项目全周期可视化工作台 — 项目总结（v7.1）

> 按用户实际推进节奏整理为四阶段：基础框架搭建 → 基础页面交互 → 文件库 → 细致优化。
> 主交付文件：`/workspace/index.html`（基线 = 用户桌面 "work buddy 版.html"）。
> 配套文档：`项目指令.md`（阶段化 brief）、`批量录入方案.md`（S4 当前子项详细方案）。

---

## 一、项目背景

### 1.1 缘起与目标

用户正在准备校招，需要一套**自己的、可随手记录的可视化工作台**，把多个求职项目（招商局、特发集团、高新投、兴邦金租等）从启动到交付的**全周期任务**统一管理起来。

核心目标：
- **收集**：录入每个项目的任务、交付物、过程留痕
- **总结**：量化自己在每个项目里完成了什么——按时间、任务类别、整体把控呈现
- **显示**：用可视化看板 + 文字总结，体现"从调研到落地的项目全周期经历"，改简历时能直接复用

最终目的：证明自己是一个**有条不紊、有深入项目全周期经历**的候选人；所有信息可随时复盘、可量化。

### 1.2 核心需求（从第一性设想中提炼）

- **自动识别录入**：项目、时间、任务类型、内容、难点、交付成果、紧急度
- **关键词模糊归一**："特发"和"特发集团"归一类；"校招启动文"和"校招简章"归为宣传类
- **母任务 / 子任务层级**：小任务折叠到母任务下，点开才显示
- **文件库**：支持飞书分享链接，点击文件名直接打开 / 下载；可与任务关联
- **工作日志自动留痕**：记录每日修改，辅助日报 / 周报
- **自动化报告**：日报 18:00、周报 周五 17:30 询问后生成，可视化 + 文字总结，可改转待办
- **全局检索**：按日期、周期、类型、关键词、难点等字段从全库调取信息
- **碎碎念**：记录感悟，不进入日报
- **数据可靠**：JSON 导入导出，换设备不丢
- **部署上线**：最终部署到 GitHub Pages / Vercel / Netlify

### 1.3 设计参考

Tokyo Tower / AMBUSH / Design Signals 海报 → poster / 编辑风：
暖米色背景（≈ #efeae0）、近黑文字（≈ #0a0a0a）、强红强调（≈ #e63946）、超大加粗标题、几何装饰、编号页眉（№01~№05）、view-enter 入场动画。

---

## 二、四阶段推进方向

| 阶段 | 主题 | 核心目标 | 状态 |
|---|---|---|---|
| **Phase 1** | 基础框架搭建 | 确定数据模型、标签体系、单页架构、视觉风格 | ✅ 完成 |
| **Phase 2** | 基础页面交互 | 五大页面落地、单条自然语言识别、筛选排序、甘特图、报告生成 | ✅ 完成 |
| **Phase 3** | 文件库 | 文件上传、飞书链接接入、点击下载/预览、文件与任务关联 | ✅ 完成 |
| **Phase 4** | 细致优化 | 批量录入、模糊索引、母/子任务、全局搜索、自动化报告、数据导入导出、部署响应式 | 🔵 **进行中** |

---

## 三、目前进度

### Phase 1 · 基础框架搭建 ✅

- 单文件 HTML+CSS+原生 JS，无构建、无后端，localStorage 持久化
- `go(route,project)` + `setMain(html)` SPA 路由，view-enter 入场动画
- `state` 数据模型：tasks / logs / notes / reports / files / projectOrder / filters / fileFilters
- 标签体系：任务内容、任务类型、项目、起始/预计/实际交付、进度、交付成果、关联任务、难点和动作、负责人、父记录、状态
- 视觉风格：poster / 编辑风，编号页眉 №01~№05
- `seed()` 示例数据 + `normalize()` 兼容旧数据

### Phase 2 · 基础页面交互 ✅

- **概况 №01**：快录框自然语言识别 → 弹窗确认 → 自动写日志；本周指标卡；今日代办；最近动态；报告生成入口
- **Roadmap №02**：项目卡片网格（可拖拽排序）；项目专属看板；MS Project 风格甘特图（周/月视图、今日线）；筛选排序栏；任务编辑/删除
- **文件库 №03**（初代）：文件快录、筛选、文件表
- **碎碎念 №04**：随手记录，不进日报
- **工作日志 №05**：自动记录每次操作，按天分组 + 搜索
- **报告生成**：日报 / 周报 / 代办，可编辑确认，确认后可转待办
- **单条识别**：`parseQuick()` 识别项目、类型、日期；`parseFile()` 识别文件字段
- **任务字段**：`version` / `feedback` 支持版本迭代留痕

### Phase 3 · 文件库 ✅

- `files` 增加 `url` 字段；`normalize` 自动补齐旧数据
- `parseFile()` 增加飞书链接正则识别：粘贴链接自动分离出文件名
- 文件录入弹窗增加「分享链接」框；必填校验：文件名 / 项目 / 文件类型 / 上传日期
- 文件表文件名带链接时渲染为蓝色可点击 `↗`，新标签页打开飞书在线预览 / 下载
- 文件表每行增加「编辑 / 删除」按钮，操作后写工作日志
- seed 内置真实飞书文档链接作示例
- 文件与任务可关联（`linkedFiles` 字段已预留）

### Phase 4 · 细致优化 🔵（进行中）

| 子项 | 内容 | 状态 |
|---|---|---|
| 4.1 批量录入 | 随手记一段不规范清单 → 自动拆成多条结构化任务 → 预览编辑 → 批量保存 | ⚪ 方案完成，代码未写 |
| 4.2 关键词模糊索引 | 项目 / 类型别名表（特发↔特发集团、校招启动文↔宣传类） | ⚪ 方案已讨论，未实现 |
| 4.3 母任务 / 子任务 | 复杂任务折叠，点开母任务显示子任务 | ⚪ 待做 |
| 4.4 Roadmap↔文件库关联 | 任务行内直接点击关联文件名下载 / 预览 | ⚪ 待做 |
| 4.5 全局搜索 | 关键词 + 全文检索，跨模块调取信息 | ⚪ 待做 |
| 4.6 自动化报告 | 定时日报 18:00 / 周报 周五 17:30，可视化 + 词云 + 可改转待办 | ⚪ 待做 |
| 4.7 数据导入导出 | JSON 导出 / 导入，换设备不丢 | ⚪ 待做 |
| 4.8 部署与响应式 | GitHub Pages / Vercel 部署，移动端适配 | ⚪ 待做 |
| 4.9 项目名确认 | 高薪投 vs 高新投 定稿，替换 seed 与识别词表 | ⚪ 待做 |

---

## 四、后续计划（Phase 4 子项）

1. **批量录入**：实现 `parseBatch()`，支持粘贴随手记后自动拆分多条任务并预览编辑
2. **模糊索引**：建立项目别名表和类型别名表，支持模糊词自动归一
3. **母任务 / 子任务层级**：用 `parent` 字段实现折叠，Roadmap 默认显示母任务
4. **任务↔文件库关联**：任务编辑弹窗可多选文件，看板/甘特图直接点击下载
5. **全局搜索**：跨 tasks / logs / notes / files 的关键词 + 全文检索
6. **自动化报告**：定时日报 / 周报，词云 + 量化图表，可编辑转待办
7. **JSON 导入导出**：数据备份与迁移
8. **部署上线**：GitHub Pages / Vercel
9. **响应式优化**：移动端可用
10. **项目名确认**：高薪投 vs 高新投 定稿

---

## 五、已知限制

- **纯前端 + localStorage**：数据仅在当前浏览器，换设备 / 清缓存会丢失（JSON 导入导出可缓解）
- **静态页无后端**：定时报告需要浏览器常开或外部触发器
- **项目名未定稿**：「高薪投 / 高新投」待确认
