# v5.5 版本说明

> 记录 `v5.html -> v5.5.html` 的改动清单。  
> 目标：在不改整体架构的前提下，补齐交互、筛选、编辑、导入导出与甘特显示能力。

## 源文件关系

- 基线：`C:\Users\15421\Desktop\v5.html`
- 目标：`C:\Users\15421\Desktop\v5.5.html`
- 结构保持：单文件静态 HTML + CSS + 原生 JS + `localStorage`

## 主要改动

### 1. 弹窗交互更稳

- 模态框关闭从单次点击改为「按下 + 抬起」双重判断，减少误关。
- 所有日期输入统一增加「当日」按钮。
- 添加任务、批量录入、编辑任务三处日期控件都补齐。

### 2. Roadmap 编辑方式调整

- 任务行支持双击直接打开编辑。
- 双击按钮、展开箭头时不触发编辑，避免误操作。
- 保留母/子任务结构和原有看板逻辑。

### 3. 任务类型改为全局管理

- 新增任务类型编辑器。
- 支持新增、重命名、删除类型。
- 删除类型后，原任务和文件自动回退到「其他」。

### 4. 甘特图显示优化

- 甘特图条形样式修复。
- 甘特图保留原有任务结构展示。
- 悬停时可看到完整信息。

### 5. 看板密度调整

- 任务名称列保持完整展示逻辑。
- 其他列压缩到更紧凑的宽度。
- 通过省略号减少横向拥挤。

### 6. 数据导入导出

- 新增整份数据导出。
- 新增数据导入。
- 导入支持覆盖与合并两种方式。
- 保留 `state` 结构兼容性。

### 7. 编辑页规则修正

- 任务类型在编辑流程中恢复可选。
- 修复甘特图条形样式一度失效的问题。

## 结果

- `v5.5.html` 作为 `v5` 到 `v5.5` 的完整升级版本。
- 核心结构不变，主要是补功能、修交互、修显示。
- 该版本可作为后续 `v6` 的功能基线。
