# v7 更新日志 · 校招全周期工作台（Work Buddy）

> 版本：**v7**
> 定位：在 №04 碎碎念页落地**本地优先、隐私可控的 AI 求职教练**，并把"AI 记忆持久化"作为核心约束。
> 入口：`work-buddy-version.html`（固定最新版）

---

## 一、本次做了什么（功能）

- **№04 碎碎念双栏改造**：左栏保留原随笔（不进日报），右栏新增「🤖 AI 小助手」对话区。
- **AI 小助手（BYOK + 透明 CORS 中继）**：用户自己粘贴 DeepSeek Key（存本地），经一层极薄中继调用 `deepseek-v4-pro`，完整读取工作台后主动梳理卡点 / 给下一步建议 / 模拟面试。
- **上下文包 `buildContextPack()`**：只在用户发消息时，把 `state` 蒸馏成结构化摘要——停滞任务、临近截止、feedback 难点、碎碎念情绪线索、最近操作——喂给模型，而非裸丢全部数据。
- **快捷提问**：梳理卡点 / 下一步建议 / 模拟面试 一键触发，降低使用门槛。
- **流式输出**：`stream:true` + SSE 解析，打字机效果，体验接近 ChatGPT。
- **⚙ AI 设置入口**：导航底部新增「AI 设置」，可填 Key / 中继地址 / 模型 / 是否包含碎碎念全文。
- **AI 记忆持久化（重点）**：AI 配置与对话历史走**独立 localStorage 键**，与 `state` 完全分离。

---

## 二、配置与部署方法（你怎么配、怎么跑通）

> 用户原话："我比较好奇你是怎么配置的，会通过什么方法" —— 这里如实记录。

### 1. 为什么不能"纯直连"
调研实测确认：**DeepSeek `api.deepseek.com` 不返回 `Access-Control-Allow-Origin`**，浏览器在 `github.io` 静态页里 `fetch` 直呼会被 CORS 拦截。所以"BYOK 直连"必须补一层**透明 CORS 中继**。

### 2. 最终架构
```
浏览器(单文件HTML) ──Bearer Key──▶ Cloudflare Worker(透明中继，只转发+加CORS头)
   │ Key 存本地、对话存本地            └──▶ api.deepseek.com (deepseek-v4-pro)
```
- Key 仍由用户持有，存 `localStorage`；中继**不存储** Key（请求透传 `Authorization`）。
- 中继是透明管道：不落库、不记对话，仅加 `access-control-allow-origin: *` 并原样转发 SSE 流。

### 3. 中继一键部署（Cloudflare Workers，免费）
- 仓库内新增 `项目资产/ai-proxy.js`（完整可部署脚本）。
- Cloudflare Dashboard → Workers & Pages → 新建 / 编辑 Worker → 粘贴脚本 → 部署；在 `Domains` 页确认 `workers.dev` 的 Worker URL 已启用（例如 `https://workspace.aaa1542116347.workers.dev`），不需要购买或绑定自定义域名。
- 设置面板「中继地址」填 Worker 根地址即可（末尾不加 `/chat/completions`，前端会自动拼接）。
- 高级用户也可把 `baseUrl` 指向任何允许 CORS 的 OpenAI 兼容端点（OpenRouter、本地 Ollama+CORS 等）。

### 4. 实机联调新增结论（2026-08-03）
- Cloudflare 后台检查结果：Worker 已有 Active deployment，`Domains` 页显示 `workspace.aaa1542116347.workers.dev` 处于 Production 且开关已启用，说明部署与免费 `workers.dev` 地址本身没有问题。
- 关键卡点不是“没买域名”，而是当前网络访问 `workers.dev` 不稳定：未挂 VPN 时本机 DNS 将 `workspace.aaa1542116347.workers.dev` 解析到异常路径，TCP 443 连接失败，页面表现为“请求似乎发出去了，但 AI 一直没有回信，也不一定出现 404”。
- 挂 VPN 后链路跑通，用户提问能够收到回复，证明 Worker 中继、DeepSeek API 与工作台前端调用链路整体可用。
- 后续排查顺序：先点 Cloudflare 后台右上角 `Visit`，能看到 `OK` 说明 Worker 可用；若本机打不开但后台部署正常，优先换 VPN / 手机热点 / 其他网络，而不是反复改代码或购买域名。
- Work Buddy 设置要求：中继地址填 `https://workspace.aaa1542116347.workers.dev`，不要带 `/chat/completions`；模型名按 DeepSeek 官方 API 可用模型填写，优先用 `deepseek-chat` 做基础联调。

### 5. 所用方法（实现侧）
- 单文件 HTML 仍是唯一形态，中继是独立部署物，**不写进 HTML**。
- 全部复用既有函数（`esc / nowISO / uid / save / openModal / setMain / pageHeader`），新增函数统一命名风格（`buildContextPack / callAI / renderAIPanel / openAISettings / loadAICfg / saveChat …`）。
- 改动通过 Python 脚本对 v6.5 源文件做精准字符串锚点插入（179KB 单文件用脚本比逐处 Edit 更可靠），非引入任何外部依赖/框架。

---

## 三、实现过程中遇到的难点（真实踩坑，已全部解决）

1. **workers.dev 未挂 VPN 访问不稳定**：Cloudflare 后台显示 Worker 已部署、`workers.dev` Worker URL 已启用，但未挂 VPN 时本机访问 `workspace.aaa1542116347.workers.dev` 的 443 端口失败，工作台表现为“发得出去但没回信”。挂 VPN 后 AI 提问能正常收到回复。结论：这是网络路径 / DNS 可达性问题，不是必须购买域名，也不是 Worker 代码必然错误。
2. **CORS 直连不通**：最初方案 A 是纯浏览器直呼 DeepSeek，实测会被跨域拦截。修正为「BYOK + 透明中继」，既跑通又守住隐私（Key 不落中继）。
3. **版本升级会覆盖 AI 记忆？** 用户明确要求"迭代升级时 AI 记忆不被覆盖"。根因分析：`localStorage` 按域名（origin）存储、与 HTML 文件版本无关；只要 AI 数据用**独立键**且不放进会被 `normalize()/seed()` 重写的 `state`，升级天然不丢。据此把 AI 配置/对话拆出 `state`，彻底解耦。
4. **导出备份会泄露 Key？** 用户要求"导出/导入时 AI 记忆还在"，但把 API Key 写进备份 JSON 有泄露风险。折中：**导出含对话记忆 + 模型/中继地址，但显式剔除 Key 字段**；换设备导入后只差 5 秒重新粘贴 Key，记忆（对话）完整迁移。
5. **单文件大改易误伤**：179KB 文件手动改风险高。改用脚本锚点插入 + 三步验证兜底（见第四节），保证不破坏既有功能。
6. **流式 SSE 解析**：`fetch` 的 `ReadableStream` 需按 `\n` 切分、保留半行 `buf`、跳过非 `data:` 行、处理 `[DONE]`，并兼容"环境不支持流式读取"时优雅降级。

---

## 四、质量保障（按项目规范：语法检查 + HTTP 200 + 断言）

1. **语法检查**：提取内联 `<script>` 跑 `node --check` → **PASS**。
2. **本地预览**：`python3 -m http.server` + `curl -I` → **HTTP 200**（194079 bytes）。
3. **针对性断言（jsdom 端到端，19 条全过）**：
   - `buildContextPack()` 返回含「停滞/整体进度」分节、读到 seed 的招商局任务；
   - 默认 `model=deepseek-v4-pro`、`key` 为空；
   - `collectAIData()` **不含 key 字段**（防泄露）；
   - `restoreAIData()` 恢复 model/baseUrl，但**绝不恢复备份里的 key**（安全）；
   - `callAI()` 无 Key 时触发 `onErr` 而非抛异常（不崩溃）；
   - 无 Key 时 №04 渲染「去设置」引导；有 Key 时出现发送键 + 3 个快捷 chip；
   - №04 已双栏布局；
   - **模拟版本升级（同 origin 重载）后 AI 对话记忆仍在**；AI 数据独立于 `state`（升级不被覆盖的根因）。

---

## 五、AI 记忆持久化设计（用户核心诉求）

| 数据 | 存储键 | 进备份导出 | 版本升级 | 换设备导入 |
|---|---|---|---|---|
| 工作台主数据 `state` | `campus_workbench_v1` | ✅ | 不变（独立键） | ✅ |
| AI 配置（key/baseUrl/model） | `campus_workbench_ai` | 仅 model/baseUrl（**剔除 Key**） | ✅ 不丢 | 配置随导入恢复，Key 需重贴 |
| AI 对话记忆 | `campus_workbench_ai_chat` | ✅ 含对话 | ✅ 不丢 | ✅ 完整迁移 |

> 结论：AI 记忆（配置 + 对话）因走独立键、不进 `state`，**版本迭代永远不会被覆盖**；换设备导出的备份里对话记忆随行，Key 出于安全单独保留在本地。

### 5.1 换浏览器 / 跨浏览器（Edge / Chrome 混用）

localStorage 的隔离维度是「**浏览器 + 网址(origin)**」，跟"这台电脑"无关。Chrome 与 Edge 各自有独立的安全沙箱，同名同 key 的三组数据互不共享。换浏览器那一刻，另一个浏览器眼里你是全新用户——工作台空、AI 无记忆。

| 你做的事 | 同一个浏览器 | 换浏览器(Chrome↔Edge) |
|---|---|---|
| 工作台任务/进度 | ✅ 在 | ❌ 不在 |
| AI 对话记忆(chat) | ✅ 在 | ❌ 不在 |
| AI 配置(模型/baseUrl) | ✅ 在 | ❌ 不在 |
| AI Key | ✅ 在 | ❌ 不在(且备份里也没有) |

**两种解法**：
1. **固定一个主力浏览器（最省心）**：给 WorkBuddy 指定一个"老家"浏览器，以后一直用它打开 `xxx.github.io`，AI 记忆 / 配置 / Key 全部原地留存，零维护。
2. **导出/导入备份搬运**：在"主库"浏览器导出备份 → 到另一个浏览器导入，可把工作台数据 + AI 对话记忆 + 模型/baseUrl 一起搬过去；**唯一搬不过去的是 Key**（备份设计上故意不导出），换浏览器后需再手动粘贴一次 Key。

> ⚠️ 若 Chrome 与 Edge 同时各写各的，备份是"最后导入那份覆盖前面"，不会自动合并。最佳实践：始终以某一个浏览器为唯一数据源，另一个只做"导入查看"。

---

## 六、CORS 逻辑（是什么、为什么一定需要那层中继）

**CORS（跨域资源共享）** 是浏览器的同源安全策略：网页（`https://suul-chen.github.io`）用 JS 去请求另一个域名（`https://api.deepseek.com`）时，浏览器会要求目标服务器在响应头里带回 `Access-Control-Allow-Origin` 表示"我允许这个来源访问"。没有这个头，浏览器直接拦截响应，前端拿不到任何数据。

**实测结论**：DeepSeek `api.deepseek.com` **不返回** `Access-Control-Allow-Origin`。所以"纯 BYOK 浏览器直呼"在 `github.io` 静态页里必被拦截——这不是 Key 或代码问题，而是浏览器的同源硬限制。

**解法就是那一薄层透明中继（Cloudflare Worker）**：
- 中继部署在 Cloudflare 提供的免费 `workers.dev` Worker URL（或你自己的自定义域名）下，并通过 CORS 头放行网页请求；
- 它只做两件事：① 原样转发请求到 `api.deepseek.com`（透传 `Authorization` 里的 Key）；② 在响应里补上 `access-control-allow-origin: *`；
- 它**不存储** Key、不记录对话，纯管道。

于是链路变成：网页 →(同源, 放行)→ 中继 →(服务端转发, 不受浏览器 CORS 限制)→ DeepSeek。浏览器只跟中继对话，CORS 由中继兜底。

> 补充：CORS 只是"浏览器到服务器"的限制；**服务端到服务端（中继 fetch DeepSeek）不受 CORS 约束**，这正是中继能成立的原因。

---

## 七、别人怎么用这个 AI（条件与限制）

这个 AI 助手是 **BYOK（Bring Your Own Key）** 模式——**谁用谁自己掏 Key、自己承担额度**，工作台本身零成本、零账号系统。

**别人能用吗？能，但有前提**：
- 他们得自己有一个 DeepSeek（或任意 OpenAI 兼容）API Key，在「AI 设置」里粘贴；
- 他们得有一个允许 CORS 的中继地址（你自己部署的，或任意 OpenAI 兼容端点）；
- 数据**完全隔离**：每个人各自的 Key、对话记忆、工作台都只存在自己浏览器的 localStorage 里，互不可见、互不干扰。

**限制 / 边界**：
- ❌ 没有 Key 就完全没有 AI 能力（只保留碎碎念随笔）；
- ❌ Key 不能借给别人（等于把你的 API 额度 / 账单给他）；
- ❌ 中继是透明管道，但 Key 仍经它透传——所以中继得是你**信任**的（自己部署最稳）；
- ⚠️ 这是 DeepSeek 的 API 额度，按 token 计费，不是"免费无限聊"；
- ⚠️ 数据全在本地，换浏览器 / 换设备 / 清缓存都会丢，靠导出备份迁移。

> 一句话：工作台随便分享，AI 能力"自带 Key 自带中继"才能用；你分享的是工具，不是你的 Key 和记忆。

---

## 八、向后兼容

- `state` / `normalize()` / `seed()` **未改动**，旧用户升级无感（AI 相关全在独立键，首次打开显示设置引导）。
- 无 Key 时 AI 区显示引导、不报错，离线可用随笔（符合向后兼容与降级规范）。
- 视觉沿用暖米底 + 强红、圆角卡片、胶囊按钮、`view-enter` 入场，无新增依赖。

---

## 九、待办 / 后续

- [x] 实机联调：Cloudflare Worker 中继 + DeepSeek Key 已跑通；未挂 VPN 时 `workers.dev` 访问失败，挂 VPN 后工作台提问可正常收到回复。
- [ ] 若用户希望"换设备也带走 Key"，可加"导出时含 Key（明文，需用户确认风险）"开关。
- [ ] 与 4.5 全局搜索、4.4 任务↔文件关联 形成闭环（AI 能引用交付物）。
