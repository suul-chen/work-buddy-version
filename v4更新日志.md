# v4 更新日志 · 校招全周期工作台（Work Buddy）

> 版本：**v4** ｜ 阶段：Phase 1 ~ Phase 4 基础能力积淀
> 定位：产品从 0 到 1 跑通"调研 → 落地 → 留痕 → 复盘"的全周期主线
> 入口：`work-buddy-version.html`（固定最新版）

---

## 一、基础框架（单文件骨架）

- **形态**：单文件纯静态 `HTML + CSS + 原生 JS`，无构建、无后端，浏览器 `localStorage` 持久化（键 `campus_workbench_v1`）。
- **SPA 结构**：`go(route,project)` + `setMain(html)` 路由；`state` 对象统一管理数据；`load() / normalize() / save()` 持久化；`seed()` 示例数据，首次打开即有可演示内容。
- **向后兼容**：任何字段新增都经 `normalize()` 补齐，旧 `localStorage` 用户升级不丢数据。
- **视觉风格**：poster / 编辑风——暖米底 `#efeae0`、近黑字 `#0a0a0a`、强红 `#e63946`、编号页眉 №01~№05、`view-enter` 入场动画。

## 二、基础交互

- **左侧导航**：概况 №01 / Roadmap №02 / 文件库 №03 / 碎碎念 №04 / 工作日志 №05 五个模块。
- **项目右键菜单（重命名 / 删除）**：在 Roadmap 的项目（导航列表或看板项目卡）上**右键**即弹出菜单，提供 `✏️ 重命名` 与 `🗑 删除项目` 两项；删除项目会一并提示其下任务 / 文件数量，避免误删。
- **Roadmap「＋」快速新增项目**：Roadmap 导航项右侧常驻 `＋` 按钮，**左键**一下即弹出新建项目，进入专属看板。
- **双视图**：表格看板 + 甘特图可切换；看板支持按任务名称 / 类型 / 状态 / 起止日期筛选与排序。

## 三、文件库

- **文件上传与识别**：粘贴信息自动识别项目、类型、上传日期、飞书分享链接。
- **文件列表与筛选**：按项目、文件类型、上传日期检索，支撑交付物沉淀。

## 四、录入与分析能力

- **批量录入**：随手记按"上下文栈"（当前项目 / 起止）拆解成多条任务，生成概览修正窗口，可逐条确认。
- **关键词模糊索引**：跨任务 / 文件的关键词检索，快速定位相关内容。
- **自动化报告**：一键生成阶段 / 项目复盘报告，把过程留痕量化成可展示成果。
- **项目名确认**：项目别名归一（`normalizeProject`），避免同一项目被写成多个名字导致数据分散。

---

## 五、已具备、本版即终态的能力一览

| 能力 | 说明 |
|---|---|
| 单文件部署 | 一个 HTML 即可运行，零依赖 |
| 项目隔离 | 各求职项目数据互不混合 |
| 看板 / 甘特 | 时间线双视图 |
| 文件沉淀 | 上传即识别、可筛选 |
| 批量拆解 | 随手记 → 多条任务 |
| 模糊检索 | 关键词跨实体定位 |
| 自动报告 | 复盘产出一键生成 |
| 项目归一 | 别名自动合并 |

---

*注：v4 为 v5 之前所有已完成工作的归集；v5 在此基础上补上母任务 / 子任务与若干体验修复，详见 `v5更新日志.md`。*
