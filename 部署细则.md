# 部署细则 · Work Buddy（work-buddy-version）

> 本文档沉淀本项目在 GitHub 上的部署方式、路径规范与避坑清单。
> 适用于任何一次新版本发布（v1 → v2 → v3 → …），可在其他对话中直接照做。

---

## 0. 一句话流程

交付新版 HTML → 上传 `vN.html` 快照 + 覆盖 `work-buddy-version.html` → 写 `vN更新日志.md` 并追加 `CHANGELOG.md` → 打 Tag `vN` + Release → 验证 Pages 返回 200。

---

## 1. 仓库与路径

| 仓库 | 用途 | Pages 地址 |
|------|------|-----------|
| **`suul-chen/work-buddy-version`** | **项目部署仓库（本细则目标）** | `https://suul-chen.github.io/work-buddy-version/` |
| `suul-chen.github.io` | 用户**个人主页**，仅含 `index.html` 个人主页 | `https://suul-chen.github.io/` |

> ⚠️ **边界**：项目文件只进 `work-buddy-version`，**绝不**塞进 `suul-chen.github.io`（那是个人主页，曾误部署后被清理还原）。

- 分支：`main`
- Pages 源：`main` 分支 / 根目录 `/`
- 入口文件：**`work-buddy-version.html`**（已删除 `index.html`，Pages 根路径会回退到它）

---

## 2. 文件命名规范

| 文件 | 规则 | 说明 |
|------|------|------|
| `work-buddy-version.html` | 每次发布**覆盖** | 始终指向**最新版**，固定访问入口 |
| `vN.html` | 每次发布**新增** | 第 N 版永久快照，只增不删 |
| `vN更新日志.md` | 每次发布**新增** | 第 N 版的阶段更新记录（原名"项目总结"） |
| `README.md` | 随内容更新 | 产品文档 |
| `CHANGELOG.md` | 每次发布**追加** | 跨版本更新日志 |

**版本号**：每发布一次 +1（v1、v2、v3 …）。`work-buddy-version.html` 跟着迭代走，`vN.html` / `vN更新日志.md` 永久留存。

---

## 3. 部署方式：GitHub Contents API（推荐，已验证）

**为什么不用 git CLI**：本沙箱全局 git 凭据助手会强制返回只读 token，覆盖我们传入的 `oauth2:<PAT>`，导致 `git push` 被 403 拦截。因此统一改用 **GitHub Contents API（Python `urllib`）**，把 PAT 直接放进 `Authorization` 请求头。

**鉴权**：使用用户提供的 **PAT（classic，勾选 `repo` 及以上 scope）**。PAT 明文**不得写入任何文件/日志/脚本**。

**核心 API**：

| 操作 | 方法 | 路径 |
|------|------|------|
| 读文件（拿 sha / 内容） | `GET` | `/repos/{owner}/{repo}/contents/{path}` |
| 新建 / 覆盖文件 | `PUT` | `/repos/{owner}/{repo}/contents/{path}` |
| 删除文件 | `DELETE` | `/repos/{owner}/{repo}/contents/{path}` |
| 打轻量 Tag（经 Release） | `POST` | `/repos/{owner}/{repo}/releases` |

请求头固定：
```
Authorization: token <PAT>
Accept: application/vnd.github+json
User-Agent: deploy-script
```

> 🔑 **中文文件名必须 URL 编码**：路径中的中文（如 `vN更新日志.md`）要用 `urllib.parse.quote(path, safe='')` 处理后再拼进 URL，否则会 404 / 编码报错。

---

## 4. 发布新版本 vN 的标准步骤

1. **校验**新 HTML：语法检查 + 本地预览 HTTP 200。
2. **上传快照**：`PUT vN.html`（新文件，无需 sha）。
3. **覆盖最新版**：`PUT work-buddy-version.html`，需先 `GET` 拿当前 sha 作为 `sha` 字段（覆盖必填）。
4. **写更新日志**：`PUT vN更新日志.md`（新文件），内容为该版本的阶段总结（按 4.1→4.9 顺序，未完成的写"后续计划"）。
5. **追加 CHANGELOG**：`GET CHANGELOG.md` → 在顶部插入一条 vN 记录（日期 / 指纹 / 变更摘要 / 状态）→ `PUT` 回去（带 sha）。
6. **打 Tag + Release**：`POST /releases`，`tag_name: "vN"`，`name: "vN"`，`body` 填更新摘要（会自动建轻量 Tag）。
7. **验证**：等 Pages 重建 1–2 分钟，访问 `https://suul-chen.github.io/work-buddy-version/` 与 `vN.html` 确认 HTTP 200。

---

## 5. Tag 与 Release

- 每次发布打 **Tag `vN`** + 创建 **GitHub Release**，作为"更新信息"留存。
- 优先用 Release 接口（一步建 Tag + Release）；若需带注释的 Tag 对象，再走 `git/tags` + `git/refs` 两步。
- 发布后 Release 页面：`https://github.com/suul-chen/work-buddy-version/releases/tag/vN`

---

## 6. 验证清单

- [ ] `vN.html` 可访问（HTTP 200，字节数与交付文件一致）
- [ ] `work-buddy-version.html` 为最新内容（与 vN 一致）
- [ ] `vN更新日志.md` 已上传
- [ ] `CHANGELOG.md` 顶部新增 vN 记录
- [ ] Tag `vN` + Release 已创建
- [ ] Pages 根路径返回 200（注意等重建完成，首查可能仍 404 / 旧构建）

---

## 7. 避坑清单（实测）

- **中文路径必须 `quote(path, safe='')`**：否则 GET/PUT/DELETE 报 404 或 `UnicodeEncodeError`。
- **DELETE 返回 409（冲突）**：说明传入的 `sha` 已失效（HEAD 因前序提交变动）。**重新 GET 拿最新 sha 再删**。
- **PUT 覆盖必须带 `sha`**：新建文件不带，覆盖已有文件必带当前 sha，否则 422。
- **非 UTF-8 文件（如 UTF-16）别用 Contents API 的 base64 来回倒**：曾出现内容被截断（64874B → 35183B）。恢复此类文件应改用 `raw.githubusercontent.com` 拉完整字节，再 base64 后 PUT。（常规部署均为 UTF-8 HTML，不受影响。）
- **Pages 重建有延迟**：推送后 1–2 分钟内线上仍是旧构建，验证请耐心等待。
- **不要碰 `suul-chen.github.io`**：那是个人主页，不属于本项目。

---

## 8. 部署脚本模板（Python）

```python
import urllib.request, json, base64, urllib.parse

TOKEN = "<用户提供的 PAT，repo scope，勿落盘>"
REPO  = "suul-chen/work-buddy-version"

def api(method, path, body=None):
    url = "https://api.github.com" + path
    data = json.dumps(body).encode() if body is not None else None
    req = urllib.request.Request(url, data=data, method=method)
    req.add_header("Authorization", "token " + TOKEN)
    req.add_header("Accept", "application/vnd.github+json")
    req.add_header("User-Agent", "deploy-script")
    req.add_header("Content-Type", "application/json")
    with urllib.request.urlopen(req, timeout=30) as r:
        return r.status, json.loads(r.read().decode())

def put_file(name, raw_bytes, msg, sha=None):
    """新建或覆盖一个文件。raw_bytes 为文件原始字节。"""
    q = urllib.parse.quote(name, safe='')
    b64 = base64.b64encode(raw_bytes).decode()
    body = {"message": msg, "content": b64}
    if sha:
        body["sha"] = sha
    return api("PUT", f"/repos/{REPO}/contents/{q}", body)

def delete_file(name, sha, msg):
    q = urllib.parse.quote(name, safe='')
    return api("DELETE", f"/repos/{REPO}/contents/{q}", {"message": msg, "sha": sha})

def get_sha(name):
    q = urllib.parse.quote(name, safe='')
    s, d = api("GET", f"/repos/{REPO}/contents/{q}")
    return d["sha"]

# 示例：发布 v5
VERSION = "v5"
html = open("work-buddy-version.html", "rb").read()

put_file(f"{VERSION}.html", html, f"deploy: {VERSION} snapshot")          # 新增快照
sha_latest = get_sha("work-buddy-version.html")
put_file("work-buddy-version.html", html, f"deploy: {VERSION} latest", sha=sha_latest)  # 覆盖最新

# 写更新日志（同理 put_file(f"{VERSION}更新日志.md", ...)）
# 追加 CHANGELOG（GET 后在顶部插一行，再 PUT 带 sha）
# 打 Release（自动建 Tag）
api("POST", f"/repos/{REPO}/releases", {
    "tag_name": VERSION, "name": VERSION, "body": "v5 更新摘要"
})
```

---

## 9. 关键地址速查

- 仓库：`https://github.com/suul-chen/work-buddy-version`
- Pages 根：`https://suul-chen.github.io/work-buddy-version/`
- 最新版：`https://suul-chen.github.io/work-buddy-version/work-buddy-version.html`
- vN 快照：`https://suul-chen.github.io/work-buddy-version/vN.html`
- vN 更新日志：`https://github.com/suul-chen/work-buddy-version/blob/main/vN更新日志.md`
- Release：`https://github.com/suul-chen/work-buddy-version/releases/tag/vN`
